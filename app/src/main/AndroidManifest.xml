<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="longxing.wshoto.com.myapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.GlideActivity"></activity>
        <activity android:name=".activity.SumActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".activity.ViewActivity" />
        <activity android:name=".activity.TestActivity" />
        <activity android:name=".activity.BackgroundActivity" />
        <activity android:name=".activity.StorageActivity" />
        <activity android:name=".activity.AndroidHeroActivity" />
        <activity android:name=".activity.HeroChapter3Activity" />
        <activity android:name=".activity.JinjieActivity" />
        <activity android:name=".jinjie.ViewActivity" />
    </application>

</manifest>